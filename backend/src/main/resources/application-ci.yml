spring:
  config:
    activate:
      on-profile: ci
  datasource:
    url: jdbc:postgresql://localhost:5432/cinema_test
    username: cinema
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    root: WARN
    ua.lviv.bas.cinema: INFO